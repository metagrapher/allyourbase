module.exports = {
    // ... other ESLint configuration ...
    rulesdir: ['./.eslint-rules'], // Path to the custom rules directory
    rules: {
      // ... other rules, such as the new rule
      'leading-commas': 'error'
    },
};
